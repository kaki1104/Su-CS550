#exponentiallist.py


#Create a program that will take two inputs (base, exponent) and will print out a list, starting from base to the 0 power to base to the [exponent] power

base = input ("base?  ")
exp = input ("exponent?  ")

l =[]
for x in range (): #(start, stop, step)
	int (base) ** num
	num = 
print (a)
